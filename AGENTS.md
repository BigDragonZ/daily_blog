# AGENTS.md

本项目是一个个人学习博客（Daily Blog）：阅读课程讲义（input/），生成前端学习页面（pages/）。
本文件沉淀跨课程不变的规则，每门课程的提示词只需写内容大纲，无需重复本文件内容。

## 学习者画像与内容风格

为零基础学习者生成内容，固定风格：

- 新概念先用反直觉示例或实际案例引出，再给定义
- 偏好图+文：图下方必须有文字解释；每个模型最后说明其优点和缺点
- 专业术语附英文原名和简写，如 比较优势（Comparative Advantage, CA）
- 章节收尾偏好横向对比总结（如四种市场结构对比表）
- 篇幅允许时给出思想链路：核心问题 → 简化模型 → 推导 → 实际应用 → 局限性

## 目录规范

- 网站页面：`pages/<学科>/<课程>/<章节>.html`
- 公共资源：`assets/css/`、`assets/js/`（图表统一复用 `assets/js/charts.js`，需要新图型时在库中扩展，不要在页面里另写绘图代码）
- 菜单数据：`assets/js/menu.js`（新增章节时在 `MENU` 中追加节点；三级菜单：学科 → 课程 → 章节）
- 原始讲义：学习中放 `input/<课程>/`，学完的章节移至 `archive/<课程>/`，父目录全部学完后删除
- 更新记录：`docs/changelog/YYYY-MM-DD-主题.md`，每次开发完成后新增一条并更新该目录 README 索引
- 提示词复盘：`docs/prompt-retro/YYYY-MM-DD-<课程>.md`，每门课程结束后新增一条

## 图表规范

以下规则从历史返工中提炼，生成图表时逐条遵守：

- 箭头不得进入方块/椭圆等图形区域，沿图形外侧走线
- 图例统一放在图片正上方
- 曲线使用足够中间路径点，保证平缓平滑，不要突兀折线
- 多阶段对比图（如贸易前后）需加汇总/合并视图（灰色虚线），并标注合计点
- 不同数据系列用不同颜色区分；福利分析类图用颜色标识各区域
- 图中标注关键点位坐标，方便正文引用分析

## 验收清单

每次开发完成后逐项核对，通过才算完成：

- [ ] 页面 figure 数量与提示词大纲一致（`--dump-dom` 计数）
- [ ] 图中几何关系与数值经计算校验（交点、均衡点等）
- [ ] 截图核对：无标签重叠、无箭头穿越、导航与右侧 TOC 正常
- [ ] 所有专业术语附中英文与简写
- [ ] `menu.js` 已注册新页面，当前页高亮正常
- [ ] `docs/changelog/` 已新增记录并更新索引
- [ ] 已学章节源文件移至 `archive/`，清空的 input 目录已删除
- [ ] 提交至 github

## 提示词迭代机制

每门课程结束后进行提示词复盘：

1. 课程进行中用户每次说"优化：…"时，除修复外，把可泛化的部分追加进本文件对应章节
2. 课程结束后写 `docs/prompt-retro/YYYY-MM-DD-<课程>.md`，只记三样：
   本次返工轮次、每轮根因、转化成的新规则（写明落入本文件哪一节）
3. 新课程提示词只写内容大纲，格式参照 `promot-template.md`
