# 2026-09-10 经济学原理 02 市场如何运行（Ch04-Ch06）

## 本次内容

- 扩展图表库 `assets/js/charts.js`（向后兼容，01 页面无需改动）：
  - 原 `Charts.ppf()` 泛化为通用 x-y 坐标图 `Charts.xy()`，并保留 `ppf`、新增 `sd` 两个别名
  - 新增 `areas` 选项：多边形填充区域（颜色/透明度/重心标签），用于消费者剩余、无谓损失等福利分析
  - 新增 `xTicks` / `yTicks` 选项：自定义刻度（数字或 `{v, label}`），传 `[]` 隐藏数字刻度
  - 标注点新增 `guides` + `gxLabel` / `gyLabel`：画到两轴的虚线参考线并在轴上标注（如 P*、Q*）
- 新增页面 `pages/economics/principles-of-economics/02-how-markets-work.html`，并在 `assets/js/menu.js` 注册：
  - Ch04-05 供给和需求：汽油价格由谁决定（反直觉引入）、市场定义与完全竞争两条件、需求量与需求定律（收入/替代效应）、个人与市场需求曲线（图 1 水平加总）、五个需求决定因素、需求变化 vs 需求量变化（图 2）、供给（图 3，结构与需求对称）、市场均衡（图 4）、短缺与过剩与供求定律（图 5）、分析均衡变化三步骤及三个图表示例（图 6 需求增、图 7 供给增、图 8 同时变化数量不确定）
  - Ch05 弹性：定义与直观类比、需求价格弹性五个影响因素、中点法计算（两个分步示例）、弹性值解读、曲线陡峭程度与两个极端（图 9、图 10）、弹性与总收入（图 11 非弹性涨价增收、图 12 弹性涨价减收）、农民反对增产技术案例、衍生弹性（交叉价格/收入/供给价格弹性汇总表）
  - Ch06 政府政策：价格上限/下限（约束与非约束、短缺与过剩、排队/歧视/黑市配给）、两张福利分析图（图 13 上限 CS:A+C/PS:E/DWL:B+D，图 14 下限 CS:A/PS:B+D/DWL:C+E）、最低工资示例、补贴替代方案；税收（单位税 vs 百分比税、对买方征税图 15、对卖方征税图 16、税收等价性、税收楔子、弹性与税负归宿、奢侈品税案例、与价格管制的比较）
- 归档：`input/001经济学原理/04~07、10、11` 六个章节源文件移至 `archive/001经济学原理/`（08、09 为 Ch07，尚未学习，保留在 input）

## 验证方式

- `node --check` 校验 charts.js / menu.js 语法
- Node 最小 DOM 桩冒烟测试：`Charts.sd` 新选项（areas/guides/自定义刻度）与 `Charts.ppf` 旧调用方式均正常
- Chrome headless 截图人工核对 16 张图（环境禁止监听端口，改用 file:// 直接渲染验证）
